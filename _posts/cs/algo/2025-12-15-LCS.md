---
title: "N-dimensional LCS(the Longest Common Sequence)"
excerpt: ""
categories: [cs, algo]
tags:
   - algorithm
   - lcs
   - dp
show_date: true
---

## 요약
여러 문자열에 대해 DP를 이용하여 가장 긴 공통 부분 수열을 찾을 수 있다.


## 아이디어
먼저 2개의 lcs를 구하는 과정을 생각해 보자. 빈 문자열과의 비교는 항상 lcs가 빈 문자열이므로 0으로 두도록 하고 index 0인 행/열을 이 값으로 채운다. 이제 일반화를 위해 두 가지 경우를 생각해 보자
1. 두 문자가 같은 경우

    이 경우는 lcs가 1만큼 증가하는 경우이다. 그런데 어떤 값에 1을 더하는지에 대해 생각할 필요가 있다. 결론적으로는 좌측 대각선 위의 값을 가져오게 된다. 나는 처음에 위, 좌측 값중 큰 값을 가져오면 된다고 생각했는데 아니었다. 내 생각에 대한 반례로 'A', 'AA'에서 각 첫, 두번째 문자 비교시를 생각해 볼 수 있다.

2. 두 문자가 다른 경우
    
    이 경우는 lcs가 증가하지 않으므로 이전의 결과를 그대로 가져오게 된다. 이번에도 어떤 값을 가져올지에 대해 생각해 보면 위, 좌측 값중 큰 값을 가져오게 된다.

이제 n차원으로 확장해 보자. n-dim vector를 생성하여 동일하게 dp를 구성할 수 있다. 모든 값이 같다면 -1 index에서 +1을 해주고, 그렇지 않다면 인접 index에서 가장 큰 값을 가져온다.

## 의사코드

```cpp
//  given n strings
//  assign n-dim vector with 0 padding at first index(it means empty string)
//  for all index
//      if all char of strings same
//          dp[a1][a2][a3][..] = dp[a1-1][a2-1][a3-1][..] + 1
//      else
//          dp[a1][a2][a3][..] = max(adjecent dp elements)

```

## 어디에씀?
- 파일 버전 관리 : lcs에 포착되지 않는 부분이 바로 변경 or 추가/삭제된 부분
- 문자열 유사도 측정
- 생물정보학 : lcs의 확장 알고리즘을 통해 DNA / RNA / 단백질 서열 비교에 쓴다고 한다.