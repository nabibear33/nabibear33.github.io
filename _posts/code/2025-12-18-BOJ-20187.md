---
title: "BOJ 20187 종이접기"
excerpt: "BOJ 20187"
categories: [code]
tags:
   - code
show_date: true
---

[https://www.acmicpc.net/problem/20187]()

## 문제 한줄요약

역순으로 생각하여 문제를 푼다. 패턴 인식으로도 풀 수 있다.


## 문제
정사각형의 종이를 중앙선을 중심으로 접는 방법은 아래 그림에서 보인 것처럼 4가지가 있다.

D: 가로 중심선을 중심으로 반으로 접되 윗 면이 아랫 면을 덮도록 접음


U: 가로 중심선을 중심으로 반으로 접되 아랫 면이 윗 면을 덮도록 접음


R: 세로 중심선을 중심으로 반으로 접되 왼쪽 면이 오른쪽 면을 덮도록 접음


L: 세로 중심선을 중심으로 반으로 접되 오른쪽 면이 왼쪽 면을 덮도록 접음


한 변의 길이가 2k인 정사각형 종이가 있을 때, 이를 세로로 k번, 가로로 k번 접으면 (접는 순서는 상관 없음) 각 변의 길이가 1인 정사각형이 된다. 아래 그림에서 보인 것처럼 각 변의 길이가 1인 정사각형의 네 귀퉁이 중 한 군데에 구멍을 낸다. 구멍의 위치는 그림에서 보인 것처럼 숫자로 표시한다.



구멍을 낸 후 접은 순서의 역순으로 종이를 펼치면, 종이에 22k개의 구멍이 있게 된다. 예를 들어, 한 변의 길이가 4(= 22)인 정사각형을 <R, D, D, R>순서대로 접은 후, 3번 위치에 구멍을 낸 다음 종이를 펼치면 아래 그림처럼 구멍이 나게 된다.



종이의 크기를 나타내는 정수 k, 종이를 접는 순서를 나타내는 정보, 구멍 뚫는 위치를 나타내는 정수가 주어질 때, 2k × 2k 격자에 뚫린 구멍의 위치를 출력하는 프로그램을 작성하시오.

첫 번째 줄에 k가 주어진다.

다음 줄에는 종이 접는 방법을 나타내는 문자가 2k개 주어지는데, 각 문자는 공백으로 구분된다. 종이를 접는 방법 D, U, R, L은 각각 해당하는 대문자 알파벳으로 주어진다.

다음 줄에는 구멍 뚫는 위치를 나타내는 정수 h(0 ≤ h ≤ 3) 가 주어진다.

접힌 종이를 접은 순서의 역순으로 펼친 후 정사각형에 뚫린 구멍의 위치를 번호로 출력한다. 출력은 총 2k줄로 이루어지며 i (1 ≤ i ≤ 2k)번째 줄에는 격자의 i번 행에 뚫린 구멍의 번호를 왼쪽에서 오른쪽 순서로, 공백을 사이에 두고 출력한다.


## 아이디어
처음에는 (0,0,상태)에서 시작하여 역순으로 배열을 늘려가려고 했다. 그런데 그러면 배열 크기가 동적이 되어버려 나는 큐나 스택을 통해 모든 값을 넣고 다음 단계에서 모든 값을 빼는 방식으로 진행하였다. 문제는 해결했으나 아이디어가 만족스럽지 못해 AI에게 조언을 구해보았는데 얘들도 못푼다... 클로드 프로요금제로는 아직 힘들어 보인다.

다른 사람들의 풀이를 보니 패턴 인식으로 훨씬 쉽게 풀 수 있다. 접기가 결국 가운데 축 기준 완전 대칭이기 때문에 특정 패턴이 반복될 수 밖에 없다. 0 1 0 1... 같이

이걸 일반화하면 가로줄은 vertical axis로 접었을 때 2배씩 늘어나므로 이에 대한 flip을 반복적으로 수행해준다. 그리고 아래 줄로 넘어갈때마다 horizontal flip을 한 번씩 수행해준다. 이걸 어떻게 생각해냈지...;;;

## 나의 풀이

{% raw %}
```cpp
#include <bits/stdc++.h>
// #define DEBUG 
using namespace std;

struct State {
    int r;
    int c;
    int value;
};


int reflect(int x, int axis) {
    int offset = abs(x - axis);
    return (x >= axis) ? (x - 2 * offset - 1) : (x + 2 * offset - 1);
}


queue<State> relocate(queue<State>& q, char dir, int& rsz, int& csz) {
    queue<State> temp;
    switch(dir) {
        case 'D':
            while(!q.empty()) {
                auto [r, c, value] = q.front();
                r += rsz;
                q.pop();
                if(value == 3) {
                    temp.push(State{r, c, 3});
                    temp.push(State{reflect(r, rsz), c, 1});
                }
                if(value == 2) {
                    temp.push(State{r, c, 2});
                    temp.push(State{reflect(r, rsz), c, 0});
                }
                if(value == 1) {
                    temp.push(State{r, c, 1});
                    temp.push(State{reflect(r, rsz), c, 3});
                }
                if(value == 0) {
                    temp.push(State{r, c, 0});
                    temp.push(State{reflect(r, rsz), c, 2});
                }
            }
            rsz *= 2;
            break;
        case 'U':
            while(!q.empty()) {
                auto [r, c, value] = q.front();
                q.pop();
                if(value == 3) {
                    temp.push(State{r, c, 3});
                    temp.push(State{reflect(r, rsz), c, 1});
                }
                if(value == 2) {
                    temp.push(State{r, c, 2});
                    temp.push(State{reflect(r, rsz), c, 0});
                }
                if(value == 1) {
                    temp.push(State{r, c, 1});
                    temp.push(State{reflect(r, rsz), c, 3});
                }
                if(value == 0) {
                    temp.push(State{r, c, 0});
                    temp.push(State{reflect(r, rsz), c, 2});
                }
            }        
            rsz *= 2;
            break;
        case 'R':
            while(!q.empty()) {
                auto [r, c, value] = q.front();
                q.pop();
                c += csz;
                if(value == 3) {
                    temp.push(State{r, c, 3});
                    temp.push(State{r, reflect(c, csz), 2});
                }
                if(value == 2) {
                    temp.push(State{r, c, 2});
                    temp.push(State{r, reflect(c, csz), 3});
                }
                if(value == 1) {
                    temp.push(State{r, c, 1});
                    temp.push(State{r, reflect(c, csz), 0});
                }
                if(value == 0) {
                    temp.push(State{r, c, 0});
                    temp.push(State{r, reflect(c, csz), 1});
                }
            }   
            csz *= 2;
            break;
        case 'L':
            while(!q.empty()) {
                auto [r, c, value] = q.front();
                q.pop();
                if(value == 3) {
                    temp.push(State{r, c, 3});
                    temp.push(State{r, reflect(c, csz), 2});
                }
                if(value == 2) {
                    temp.push(State{r, c, 2});
                    temp.push(State{r, reflect(c, csz), 3});
                }
                if(value == 1) {
                    temp.push(State{r, c, 1});
                    temp.push(State{r, reflect(c, csz), 0});
                }
                if(value == 0) {
                    temp.push(State{r, c, 0});
                    temp.push(State{r, reflect(c, csz), 1});
                }
            }          
            csz *= 2;
            break;
    }
    return temp;
}


int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    int k;
    cin >> k;
    stack<char> s;
    for(int i = 0; i < 2 * k; i++) {
        char c;
        cin >> c;
        s.push(c);
    }
    int h;
    cin >> h;
    queue<State> values;
    values.push({0,0,h});

    int rsz = 1, csz = 1;
    while(!s.empty()) {
        char cur = s.top();
        s.pop();
        values = relocate(values, cur, rsz, csz);
        
    }
    vector<vector<int>> v(rsz, vector<int>(csz, 0));
    while(!values.empty()) {
        auto [r, c, value] = values.front();
        values.pop();
        v[r][c] = value;
    }
    for(auto& row: v) {
        for(int x: row) {
            cout << x << " ";
        }
        cout << "\n";
    }
}

```
{% endraw %}

## 다른 사람 풀이
```cpp
#define _CRT_SECURE_NO_WARNINGS
#include <iostream>

using namespace std;

int invH[4] = { 1, 0, 3, 2 };
int invV[4] = { 2, 3, 0, 1 };
int H, V;

int main() {
	char c;
	int K, N, i, j;
	scanf("%d", &K);
	N = 1 << K;

	for (i = 0; i < 2 * K; ++i) {
		scanf(" %c", &c);		
		if (c == 'D') V = 1;
		else if (c == 'U') V = 0;
		else if (c == 'R') H = 1;
		else H = 0;
	}

	int hole;
	scanf("%d", &hole);	
	if (H) hole = invH[hole];
	if (V) hole = invV[hole];

	for (i = 0; i < N; ++i) {		
		for (j = 0; j < N; ++j) {
			printf("%d ", hole);
			hole = invH[hole];
		}
		printf("\n");		
		hole = invV[hole];		
	}

	return 0;
}
```
