---
title: "BOJ 2957 이진 탐색 트리"
excerpt: "BOJ 2957"
categories: [code]
tags:
   - code
   - tree
   - map
show_date: true
---

[https://www.acmicpc.net/problem/2957]()

| 타입     | 체크 |
|:--------:|:----------:|
| 도움 없이 품?        |  |
| 알고리즘 분류 힌트 봄?|  |
| AI/게시판 도움까지 받음?    | O |
{: style="margin-left: auto; margin-right: auto;"}


## 문제
이진 탐색 트리는 모든 노드가 많아야 2개의 자식 노드를 가지고 있는 트리이고, 각 노드에는 수가 하나씩 쓰여있다. 만약 어떤 노드에 쓰여 있는 수가 X라면, 그 노드의 왼쪽 서브트리에는 X보다 작은 수, 오른쪽 서브트리에는 X보다 큰 수만 저장되어 있어야 한다.

1보다 크거나 같고, N보다 작거나 같은 수 N개가 한 번씩 등장하는 수열이 입력으로 주어진다. 이 수열을 이용해서 이진 탐색 트리를 만들려고 한다. 이제 배열의 첫 번째 수를 루트 노드로 놓고, 다른 나머지 수들을 순서대로 삽입하면서 이진 탐색 트리를 만들려고 한다. 즉, 첫 번째 수를 제외한 모든 수에 대해서 insert(X,root)를 실행하는 것과 같다. 그 함수는 다음과 같다. 

이진 탐색 트리에 삽입하는 함수는 다음과 같다.

```
insert(number X, node N)
    카운터 C값을 1 증가시킨다
    if X가 노드 N에 있는 수보다 작다면
        if N의 왼쪽 자식이 없다면
            X를 포함하는 새 노드를 만든 뒤, N의 왼쪽 자식으로 만든다
        else
            insert(X, N의 왼쪽 자식)
    else (X가 노드 N에 있는 수보다 크다면)
        if N의 오른쪽 자식이 없다면
            X를 포함하는 새 노드를 만든 뒤, N의 오른쪽 자식으로 만들기
        else
            insert(X, N의 오른쪽 자식
```
각 수를 삽입한 후에 C의 값을 출력하는 프로그램을 작성하시오. 카운터 C의 값은 0으로 초기화되어 있다.

입력
첫째 줄에 수열의 크기 N이 주어진다. (1 ≤ N ≤ 300,000)

다음 N개의 줄에는 수열의 수가 차례대로 주어진다. 수는 구간 [1, N]에 포함된 정수이고, 중복되지 않는다.

출력
N개의 줄에 각 수가 트리에 삽입된 후에 카운터 C값을 한 줄에 하나씩 출력한다.

## 아이디어
처음에는 단순히 링크드 리스트 형태의 노드를 클래스로 구성하고 이어붙이려 했으나 이 경우 내림차순, 오름차순에 대해 $O(n^2)$의 결과를 초래할 수 있다. 따라서 $O(n\log n)$임에도 카운트를 다 할 수 있는 방법을 생각해야 한다. 기본적으로 set, map이 그런 트리 구조로 되어 있다고 한다.

핵심 아이디어는 count 횟수가 요소의 tree에서의 depth와 같다는 점이다. 따라서 map에 저장된 depth만 구하면 되는데 여기서 한 가지 더, 요소 기준 인접한 값 둘 중 하나는 반드시 부모 요소가 된다는 점이다. 따라서 두 값을 구하고 max를 취해준다. 여기에서 엣지 케이스로 값이 가장 큰 경우와 가장 작은 경우에 대해 케이스를 나누어 처리해준다. 마지막으로 count가 prefix sum의 형태이기 때문에 자료형에 주의한다.

```cpp
//  declare map<value, depth of node>
//  for all values:
//      calculate upper bound ptr(successor)
//      get prev ptr(predecessor)
//      deal with edge cases(fully incremental/decremental)
```

## 시간 초과 나는 링크드 리스트
```cpp
#include <bits/stdc++.h>
// #define DEBUG 
using namespace std;


class Node {
    private:
        int value;
        Node *parent;
        unique_ptr<Node> left_child;
        unique_ptr<Node> right_child;
        inline static int counter = 0;
        
    public:
        Node(int v_, Node *p_) : value(v_), parent(p_) {}
        void insert(int x);
        void print_counter();
};
    

void Node::insert(int x) {
    counter++;
    if(x < value) {
        if(left_child == nullptr) {
            left_child = make_unique<Node>(x, this);
        }
        else {
            left_child->insert(x);
        }
    }
    else if(x > value) {
        if(right_child == nullptr) {
            right_child = make_unique<Node>(x, this);
        }
        else {
            right_child->insert(x);
        }
    }
}


void Node::print_counter() {
    cout << counter << "\n";
}


int main() {
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int N;
    cin >> N;
    
    int val;
    cin >> val;
    Node n(val, nullptr);
    n.print_counter();
    N--;
    while(N--) {
        cin >> val;
        n.insert(val);
        n.print_counter();
    }
    return 0;
}
```

## 최종

```cpp
#include <bits/stdc++.h>
// #define DEBUG 
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    
    int N;
    cin >> N;
    
    int val;
    long long count = 0;
    map<int, int> m;
    while(N--) {
        cin >> val;
        int depth;
        if(m.empty()) {
            depth = 0;
        }
        else {
            auto ub = m.upper_bound(val);
            if(ub == m.end()) {
                // val is the most largest number
                ub--;
                depth = ub->second + 1;
            }
            else if(ub == m.begin()) {
                // val is the most smallest number
                depth = ub->second + 1;
            }
            else {
                auto prev_ub = prev(ub);
                depth = max(prev_ub->second, ub->second) + 1;
            }
        }
        m.insert({val, depth});
        count += depth;
        cout << count << "\n";
    }
    return 0;
}
```